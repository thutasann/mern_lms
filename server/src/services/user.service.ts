import { UserRegisterBody } from '../core/dto/user.dto';

/**
 * User Service
 */
export class UserService {
	/** create user */
	async createUser(body: UserRegisterBody) {
		try {
		} catch (error) {}
	}
}
